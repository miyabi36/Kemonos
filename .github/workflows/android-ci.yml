name: Android CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
  workflow_dispatch:

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Gradle cache
        uses: gradle/actions/setup-gradle@v3

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build Debug
        run: ./gradlew assembleDebug

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest

      # testDebugUnitTest exists only in Android modules. JVM-only modules
      # (core:utils, the *-api modules) keep their tests under `test`, so
      # without this step they never run.
      - name: Run JVM module unit tests
        run: ./gradlew test -x testDebugUnitTest

      - name: Build Release (no signing)
        run: ./gradlew assembleRelease
